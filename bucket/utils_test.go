package bucket

/*
import (
	"archive/tar"
	"compress/gzip"
	. "github.com/smartystreets/goconvey/convey"
	"os"
	"testing"
)

func TestUtils(t *testing.T) {
	Convey("Test Utils ", t, func() {
		Convey("Test Extract tar.gz file", func() {
			os.Mkdir("test", 0755)
			f, _ := os.Open("test.tar.gz")

			gr, _ := gzip.NewReader(f)
			defer gr.Close()
			tr := tar.NewReader(gr)
			//Just Extract to tmpDir
			Extrat(tr, "test/")
			//get ids

		})

	})
}
*/
